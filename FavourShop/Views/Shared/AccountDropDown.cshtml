<a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    Категорически приветсвуем, @(User.Identity.Name ?? "User")!
</a>
<div class="dropdown-menu" aria-labelledby="navbarDropdown">
    @if (!User.Identity.IsAuthenticated)
    {
        <a asp-action="login" asp-controller="account" class="dropdown-item">Логин</a>
        <a asp-action="register" asp-controller="account" class="dropdown-item">Регистрация</a>
    }
    else
    {
        <a asp-action="myOrder" asp-controller="order" class="dropdown-item">Мои заказы</a>
        <form method="post" asp-action="logout" asp-controller="account" class="no-padding-margin dropdown-item">
            <button class="btn dropdown-item btn-no-effect">Выйти</button>
        </form>

        if (User.IsInRole("Admin"))
        {

            <div class="dropdown-divider"></div>
            <a class="dropdown-item" asp-action="AllOrders" asp-controller="admin">Управление Заказами</a>
            <a class="dropdown-item" asp-action="ManageFavours" asp-controller="admin">Управление Товарами</a>
        }
    }
</div>